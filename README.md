# Spark 2 Standalone with Docker

Docker file for building a container with Spark 2 in Standalone mode

## Build image
```bash
docker build -t djodar/ubuntu -f DockerFile .
```

## Run image
```bash
docker run -it djodar/ubuntu
```